# Supabase (optionnel) — auth, sync cloud, gestion des abonnements.
# Sans ces variables, l'app tourne en 100 % local (localStorage).
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Stripe (optionnel) — à brancher pour le paiement réel du plan Pro.
# STRIPE_SECRET_KEY=
# NEXT_PUBLIC_STRIPE_PRICE_PRO=
